# OS / editor
.DS_Store
.vscode/
.idea/

# Python / Django (backend/)
venv/
.venv/
__pycache__/
*.pyc
*.sqlite3
db.sqlite3-wal
db.sqlite3-shm
.pytest_cache/
staticfiles/
media/

# Redis snapshot written into backend/ by the local broker
dump.rdb

# scripts/dev.sh runtime dir (per-service logs + redis working dir)
.dev/

# Node / Vite (frontend/)
node_modules/
dist/
frontend/.vite/

# Env files (both apps)
.env
.env.local

# Teaching workspace (/teach) - local learning material, not part of the app
learning/
